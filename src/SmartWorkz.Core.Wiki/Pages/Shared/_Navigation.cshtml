@inject WikiDocumentService WikiService

@{
    var categories = WikiService.GetCategories();
}

<div class="wiki-nav p-3">
    <div class="accordion accordion-flush" id="wikiAccordion">
        @foreach (var category in categories)
        {
            <div class="accordion-item bg-dark border-secondary">
                <h2 class="accordion-header">
                    <button class="accordion-button accordion-button-dark collapsed text-white"
                            type="button"
                            data-bs-toggle="collapse"
                            data-bs-target="#collapse-@category.Slug"
                            aria-expanded="false">
                        @category.Name
                        <span class="badge bg-secondary ms-auto">@category.Documents.Count</span>
                    </button>
                </h2>
                <div id="collapse-@category.Slug"
                     class="accordion-collapse collapse"
                     data-bs-parent="#wikiAccordion">
                    <div class="accordion-body p-0">
                        <div class="list-group list-group-flush">
                            @foreach (var doc in category.Documents)
                            {
                                <a href="/doc?slug=@doc.Slug"
                                   class="list-group-item list-group-item-action list-group-item-dark text-white small">
                                    @doc.Title
                                </a>
                            }
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
