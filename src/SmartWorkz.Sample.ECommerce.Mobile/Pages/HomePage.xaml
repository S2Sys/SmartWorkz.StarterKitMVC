<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SmartWorkz.ECommerce.Mobile.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Products"
    BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#212121}">

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="*">

        <!-- Error Message -->
        <Label
            Grid.Row="0"
            Text="{Binding ErrorMessage}"
            TextColor="Red"
            IsVisible="{Binding IsError}"
            Padding="16,8"
            FontSize="12" />

        <!-- Loading Indicator -->
        <ActivityIndicator
            Grid.Row="0"
            IsRunning="{Binding IsBusy}"
            IsVisible="{Binding IsBusy}"
            Color="{StaticResource Primary}"
            Margin="0,8" />

        <!-- Product Grid -->
        <RefreshView
            Grid.Row="1"
            Command="{Binding RefreshCommand}"
            IsRefreshing="{Binding IsBusy}">

            <CollectionView
                ItemsSource="{Binding Products}"
                EmptyView="No products available."
                SelectionMode="Single"
                SelectionChangedCommand="{Binding SelectProductCommand}"
                SelectionChangedCommandParameter="{Binding SelectedItems[0], Source={RelativeSource Self}}">

                <CollectionView.ItemsLayout>
                    <GridItemsLayout
                        Orientation="Vertical"
                        HorizontalItemSpacing="8"
                        VerticalItemSpacing="8"
                        Span="{Binding ColumnCount}" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="8">
                            <Frame
                                CornerRadius="12"
                                Padding="12"
                                BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#404040}"
                                HasShadow="True">

                                <VerticalStackLayout Spacing="8">
                                    <!-- Product Image Placeholder -->
                                    <BoxView
                                        HeightRequest="150"
                                        CornerRadius="8"
                                        BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#333333}" />

                                    <!-- Product Name -->
                                    <Label
                                        Text="{Binding Name}"
                                        FontSize="14"
                                        FontAttributes="Bold"
                                        TextColor="{AppThemeBinding Light=#000000, Dark=#FFFFFF}"
                                        LineBreakMode="TailTruncation" />

                                    <!-- Product Price -->
                                    <Label
                                        Text="{Binding Price, StringFormat='{0:C}'}"
                                        FontSize="16"
                                        FontAttributes="Bold"
                                        TextColor="{StaticResource Primary}" />

                                    <!-- Stock Status -->
                                    <Label
                                        Text="{Binding Stock, StringFormat='Stock: {0}'}"
                                        FontSize="12"
                                        TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}" />

                                    <!-- View Details Button -->
                                    <Button
                                        Text="View"
                                        Padding="12,8"
                                        CornerRadius="6"
                                        FontSize="12"
                                        Margin="0,4,0,0" />
                                </VerticalStackLayout>
                            </Frame>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
