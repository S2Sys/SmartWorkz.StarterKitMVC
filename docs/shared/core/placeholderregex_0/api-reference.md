# PlaceholderRegex_0 API Reference

## Classes & Interfaces

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

### RunnerFactory

- **Namespace:** `System.Text.RegularExpressions.Generated.PlaceholderRegex_0.RunnerFactory`
- **Summary:** Provides a factory for creating  instances to be used by methods on .

#### Methods & Properties

- **CreateInstance** - Creates an instance of a  used by methods on .
- **Runner.Scan** - Scan the  starting from base.runtextstart for the next match.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
- **Runner.TryFindNextPossibleStartingPosition** - Search  starting from base.runtextpos for the next location a match could possibly start.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if a possible match was found; false if no more matches are possible.
- **Runner.TryMatchAtCurrentPosition** - Determine whether  at base.runtextpos is a match for the regular expression.
  - Parameters:
    - `inputSpan`: The text being scanned by the regular expression.
  - Returns: true if the regular expression matches at the current position; otherwise, false.

